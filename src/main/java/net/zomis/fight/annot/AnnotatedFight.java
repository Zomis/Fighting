package net.zomis.fight.annot;

public class AnnotatedFight {

	public AnnotatedFight() {
		
	}
	
	
	/*
	 * 
	 * - read annotations of an object
	 * - create indexes and stuff
	 * - register notificators
	 * - if @Data is on a `ToIntFunction`, then use a IntSummary collecting mechanism
	 * - if @Data is on a `Function` then use Map<String, Integer>
	 * - if @Data is on a `Predicate` then use IntSummary?
	 * 
	 */
	
	
}
